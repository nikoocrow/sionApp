import 'package:flutter/material.dart';


class AnunciosScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
        title: Text('Anuncios'),
      ),
      body: Center(
        child: Text('Hola Anuncios'),
     ),
   );
  }
}